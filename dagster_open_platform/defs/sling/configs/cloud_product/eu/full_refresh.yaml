source: CLOUD_PRODUCTION_EU_MAIN
target: SLING_DB_EU_MAIN

streams:
  public.users_organizations:
    mode: truncate
    sql: |
      select id
      from users_organizations
    object: '{stream_table}_current_state_ids'
    meta:
      dagster:
        asset_key: sling.cloud_product_eu.users_organizations_current_state_ids
